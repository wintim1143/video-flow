# 2026-09-08

## 串行化 + 转场衔接（尾帧链）

用户反馈：分镜转场生硬（口红镜1→模特镜2无衔接），要求改串行 + 调研 GitHub 参考。

调研结论（GitHub 一手项目）：
- ai-video-pipeline（Youhai020616）：**尾帧链（tail-frame chaining）**——人物镜头串行，每镜截末帧作下一镜首帧参考，空镜才并行。直接可抄。
- StoryMem（字节+NTU，Kevin-thu/StoryMem）：逐镜生成 + 记忆库（前面镜头关键帧注入后续生成条件），Wan2.2 + LoRA。
- HoloCine（港科大+蚂蚁）：整体生成策略，转场由导演层统一规划。

落地改动：
- `shots/route.ts`：CONCURRENCY 2→1（串行流水线，neighbors 接力才有效）；fixed 强制覆盖 transition_out（大纲为准）。
- `schema.ts`：ShotOutlineItemSchema 加 `transition_out`（大纲阶段规划转场）。
- `prompts.ts`：
  - outlineSystemPrompt 规则 1 加 transition_out 字段；新增规则 12「转场衔接规划（最高优先级）」：同场景必须匹配剪辑/首尾帧衔接（写明动势如何承接），换场才允许硬切/叠化，禁止生硬跳切；JSON 结构加 transition_out。
  - shotDetailSystemPrompt 规则 7 改为「transition_out 照抄大纲」；新增规则 9「转场衔接（尾帧链）」：video_prompt 末帧=end_state 且动势方向与下一镜首帧动作匹配；换场转场自然收尾。
  - shotDetailUserPrompt：加【本镜转场设计】段（照抄提示）；上一镜 image_prompt 参考从截 120 字符加到 240。
- `shot/route.ts`：transition_out 同样大纲强制覆盖。
- `exports.ts`：toMarkdown 加「尾帧链工作流」提示（串行生成→截末帧→下一镜首帧参考）；allVideoPrompts 头部注明按镜序串行生成。

tsc 通过。待用户用口红 brief 重测验证转场效果。

## 秒级节拍（beats）

用户发现 video_prompt 里出现 "0-3s" 粗粒度时段描述、所有动作挤在一个镜头里。落地秒级节拍：
- `schema.ts`：ShotSchema 加 `beats: string[]`（英文，"0-1s: ..." 格式，覆盖 0→duration，末拍可短）+ `beats_cn`（中文对照）。
- `prompts.ts` shotDetailSystemPrompt 新增规则 10：每秒一拍（duration<2s 拆 2 拍），每拍一个连续小动作，首拍从 start_state 出发、末拍结束于 end_state，拍间状态直接接续；video_prompt 按 beats 串联但禁止再写粗粒度时段；JSON 结构加 beats/beats_cn。
- `ShotRow.tsx`：新增 BeatsBlock（EN/中文随语言切换、逐行可编辑、可复制），挂在 Video Prompt 下方；patchList 辅助函数。
- `exports.ts`：allVideoPrompts 每镜追加 `Second-by-second beats: ...`；shotMd 加秒级节拍（EN+中文）代码块。
- 旧分镜数据无 beats 字段，UI 用 `?? []` 兜底。
- 验证：tsc 前两次被沙箱 SIGTERM(137)，改后台跑通（43s），0 真实代码错误。

## goodcase skill 安装 + 长格式对比

- 用户给了 `npx skills add LearnPrompt/goodcase-lite --skill goodcase --global`（goodcase.ai 爆款案例查询 skill）。npx 安装被沙箱网络代理拦（git clone CONNECT 502，exit 137），改为 WebFetch 审计原文后手动装入 `~/.workbuddy/skills/goodcase/SKILL.md`。审计结论：纯 curl 查询公开 API（goodcase.ai/api/public，无 Key、无脚本、无凭据），P2 安全。skill 功能：查 AI 爆款案例（promptFull/稳定分/成本档）+ 模型评测工作流。
- 用户贴了 MiniMax h3 案例格式（12s 变装视频单条长 prompt）：结构 =【时长/画幅/风格/语言/人物数量】头 + 人物角色卡 + 转场前状态/转场后状态 + 核心反差 + 逐镜头（绝对时间戳 + 0.2-0.5s 粒度微动作）+ 声音设计（分镜级音效）+ 主要约束。
- 关键洞察：该格式是「整片单 prompt」工作流（MiniMax h3 一次生成 12s 多镜头），与我们「逐镜 prompt + 尾帧链」互补。gap：我们缺整片头段（风格/语言/人物数）、角色卡语义（参考图锁定身份）、全局前后反差段、声音时间轴、约束清单段。
- 已向用户提议：新增「整片长 Prompt 导出」（按该格式拼 storyboard，含 0.5s 细粒度节拍），待确认开工。

## 系统输出 vs goodcase 案例对比实验（用户指定：App 推广 + 商品推广两条 brief 双跑）

- 探针基建：`.probe/`（gitignored）——复制 src/lib 到 .probe 并 perl 补相对导入 .ts 后缀，`node --experimental-strip-types` 直跑完整链路（风格→大纲→逐镜串行），正常落 trace 日志。复用此法做链路验证，绕开 dev server 无法启动的限制。
- 生成结果：App（2 镜，tmtsrion5hszu）/ 商品（3 镜，tmtsrlemppfn7），存 /tmp/vf-compare-result.json；对照 goodcase 案例（水光肌精华 stab90 / 播放器录屏 case）存 /tmp/goodcase-*.json。
- **重要发现 1（lanfengai 400）**：temperature+response_format+reasoning_effort 组合返回不带参数名的 invalid_request，单项均 200。已在 src/lib/llm.ts 加通用 400 逐级降级（先剥 response_format 再剥 effort），tsc 无真实错误。
- **重要发现 2（范式错配）**：App 功能演示的正确范式是「真机录屏 + AI 只动画布」（UI 像素级锁定），分镜式生成的 UI 必然是假界面；video-flow 适合 App 品牌情绪片。商品推广则两套互补：单段式适合 Seedance 整片快速出片，分镜式适合精确节奏/跨镜一致。
- 输出瑕疵：keywords_en verbatim 注入无去重（"clean ivory background" 重复两次）；video_prompt 超长（~200 词 vs 规则 40-80）。待修。
- 对比报告：project-docs/20260908_两套提示词方案对比.md

- 盲测评分包：prompt-eval/（app_A/B、product_A/B、README 含评分维度与答案揭晓）。A/B 交错分配避免模式偏置：app_A=goodcase 录屏案例、app_B=本系统；product_A=本系统、product_B=goodcase 水光肌案例。用户将投喂其他 LLM 评分。
