# 2026-09-10

## 颗粒度理念修正（用户拍板）
- beats 不应固定每秒一拍，粒度应由 LLM 按动作复杂度自适应：密集动作可到 0.5s 级，单一动作可整镜一拍。prompts.ts 规则 10 已改为自适应版。

## Agnes Video 2.5 Flash 验证（未完成，见 blockers）
- llm.config.json video 组：baseURL=https://apihub.agnes-ai.com/v1，model=agnes-video-2.5-flash，endpoint=/videos；name 已从复制错误的"某生图服务"改为"Agnes Video 2.5 Flash（视频生成）"。
- 实测（curl 探测，文档页 www.agnes-ai.com 被安全审批拦截无法抓取）：
  - GET /v1/models 200，模型列表含 agnes-video-2.5-flash（OpenAI 兼容类型）→ key 有效。
  - POST /v1/videos 存在；必填：model、prompt、mode（报错顺序：无model→无prompt→无mode→invalid mode）。
  - GET /v1/videos 404（轮询地址不是这个，同步返回或另有查询端点）。
  - 免费档限流约 1 次/分钟，且首次真实生成即触发 rate_limit_exceeded → 无法端到端验证出片。
- blockers：mode 的合法值未知（试过 text 无果即限流）。等用户提供文档里的请求示例（mode 取值），或下次批准抓取文档页。
