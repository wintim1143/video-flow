# 2026-09-07

## 链路日志（轻量观测）方案 A 落地

用户需求：LLM 调用需要类似链路追踪的日志，网页可看，用于微调时对比。对比后选定方案 A（自建 JSONL + 内置 /logs 页），未选 Langfuse/Phoenix（本地单人工具，不值得上 Docker）。

实现：
- `src/lib/trace-log.ts`：零依赖落盘层，追加 JSONL 到 `.data/traces.jsonl`（gitignored）；`TRACE_LOG_DIR` 环境变量可覆盖目录（测试专用）。记录字段：traceId/step/model/temperature/hasImage(布尔不落base64)/attempts/degraded(降级参数)/ok/latencyMs/raw/error/usage/system/user。含 `exportFineTune()` 微调格式导出（messages+completion）。
- `src/lib/llm.ts` chatJSON 加 `trace?: {traceId, step}` 参数：`writeLog` 闭包在成功/失败两路径统一落一条；降级队列每轮 attempts++ 并 push 降级参数名；新增 `flattenUserContent` 把多模态 parts 拍平为 `[image] + 文本`。
- 路由接入：shots（traceId=每请求，step=outline/shot_N）、style（style / style_from_image）、shot（shot_retry:N）。
- `src/app/api/logs/route.ts`：GET 列表（traceId/limit 过滤）、`?export=1` 下载微调 JSONL。
- `src/app/logs/page.tsx`：列表+单条详情（prompt/原始输出/错误/降级）+ 勾选两条左右对比 + 导出按钮；主页顶栏加「链路日志」入口。

验证：tsc 无真实代码错误（TS7016 next 类型噪音为沙箱 node_modules 残缺，与之前一致）；/tmp 探针 13/13 过（写入/读回/排序/过滤/导出/坏行容错，`node --experimental-strip-types` 直跑 TS，trace-log.ts 特意保持零项目内依赖以支持该方式）。

硬规则再次生效：探针 fixture 全在 /tmp，TRACE_LOG_DIR 指向 /tmp，未触碰项目根 llm.config.json。
未提交（等用户指示）。

## 默认时长改 5s + 口红测试 prompt

- `src/app/page.tsx`：DURATION_PRESETS 改为 [5,15,30,60]、useState(5)、number 输入兜底 || 5；`src/lib/schema.ts`：targetDuration default(30)→default(5)。5s 会 clamp 到最少 2 镜（page suggestShotCount min 2）。
- 给了用户一条口红广告测试 brief（直接粘贴到需求框，5s / 9:16）。

## 日志排查三问题（23:58）

用户反馈三问题，用 traces.jsonl 定位：
1. **生图入口**：风格 tab 底部「风格样张 · 生图确认」卡片（StyleTestImage），配好 image LLM 后有「生成样张」按钮——入口不显眼导致用户没找到，属 UI 引导问题。
2. **5s 默认**：日志证明已生效（outline meta target_duration=5，2 镜×2.5s，sum=5.0）；真正遗漏是 DURATION_PRESETS 漏加 5 按钮（上轮说改没改），本轮补上 [5,15,30,60]。「感觉时长长」另一原因是 video_prompt 无明确时长数字，豆包默认 5s/10s。
3. **复制 prompt 缺前置 + 主体不贯穿**：exports/ShotRow 复制的是纯正文。修复：单镜视频 prompt 复制加前缀【广告视频·画幅·本镜时长】（ShotRow 加 copyPrefix + aspectRatio prop）；allVideoPrompts 加全局头；toMarkdown 加「类型：广告」；outlineSystemPrompt 加规则 11「主体贯穿每一镜」（产品入画或与产品明确互动，禁止首镜后消失）；shotDetailSystemPrompt 规则 3 改为必须写明确时长数值 + 规则 5 主体突出。

附带发现：DS（deepseek-v4-flash）单镜 reasoning_tokens 高达 10-15k（80-120s/镜），reasoning_effort=minimal 似乎未生效或 DS 忽略；建议用户在 llm.config.json 给 DS 配 `reasoningEffort: ""` 实验关闭思考。tsc 通过。
