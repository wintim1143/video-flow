# 项目待完善清单（代码审查）

日期：2026-09-12 ｜ 范围：src/（3837 行）、配置、文档 ｜ 依据：代码实读 + git 历史核对

## P0 — 明确的回归缺陷

| # | 问题 | 证据 | 影响 |
|---|---|---|---|
| 1 | **默认时长回到 30s**（用户明确要求 5s） | `src/app/page.tsx:80` `useState(30)`、`:81` `suggestShotCount(30)`；而 `src/lib/schema.ts:142` 已是 `default(5)` | 打开页面即 30s，与用户预期不符；前后端默认值不一致。`git log -S "useState(5)"` 查无记录 → 改动从未落地到仓库 |

## P1 — 影响可用性/可维护性

| # | 问题 | 证据 | 建议 |
|---|---|---|---|
| 2 | **上游 429/5xx 无重试** | `src/lib/llm.ts:207-224` 仅对 400 参数错误做降级；429（免费档限流）与 5xx 直接抛 UPSTREAM → 502 | 对 429/503 加指数退避重试（2-3 次，尊重 Retry-After）；串行场景下风险低 |
| 3 | **零测试基建** | `package.json` 仅 dev/build/start/typecheck；此前的验证全靠一次性 /tmp 探针（已删） | 引入 vitest（无 DB、纯函数优先）：prompts 组装、schema 容错、exports 格式、trace-log 读写、llm 降级链 —— 这五块是最易回归的核心逻辑 |
| 4 | **README 与实际严重失同步** | README:52 仍写「并发 2」（实际 `CONCURRENCY=1` 串行）；目录结构列了不存在的 `StyleCard.tsx`，漏掉 logs/trace-log/api-logs/api-image/api-shot/api-llm-configs/ProgressModal/StyleEditor/StyleTestImage/EditableText；未提及链路日志、秒级节拍、尾帧链转场、复制前缀、默认时长 | 重写「分镜生成」与「目录结构」两节，补 4 个新功能说明 |
| 5 | **M0 概览文档同样未更新** | `M0-里程碑概览.md` 无节拍/日志/串行等描述 | 与 README 同步 |

## P2 — 体验与健壮性

| # | 问题 | 证据 | 建议 |
|---|---|---|---|
| 6 | **工作状态不持久化** | `page.tsx` 全文无 localStorage；837 行单文件组件、20+ 个 useState | 关键状态（brief/风格/分镜/时长）落 localStorage，刷新不丢；顺带把 830 行拆成 hooks + 子组件 |
| 7 | **死代码** | `prompts.ts:98-161`（shotsSystemPrompt/shotsUserPrompt，~64 行）无任何引用，两段式改造遗留；`page.tsx:35` 的 suggestShotCount 与 `prompts.ts:164` 重复实现 | 删除死代码；shotCount 统一从 lib 导入 |
| 8 | **trace 日志无上限** | `trace-log.ts:85-105` 全量读 + reverse；`traces.jsonl` 只增不减，且每条含完整 prompt | 按月分文件或按大小轮转；`/api/logs` 改为尾部读取（只解析最后 N 行） |
| 9 | **导出措辞与新语义不符** | `exports.ts:22` `Second-by-second beats:`、`:44`「秒级节拍」——beats 已改为**自适应粒度**（可 0.5s 也可整镜一拍） | 改成 `Beats:` / 「节拍」 |
| 10 | **`.env.example` 缺项** | 代码支持 `LLM_REASONING_EFFORT`、`LLM_VISION`（`llm-configs.ts:96-106`），模板无说明 | 补两行注释 |

## P3 — 已知功能缺口（此前讨论未落地）

| # | 缺口 | 背景 |
|---|---|---|
| 11 | **「整片单 prompt」导出** | 对比实验结论：Seedance/MiniMax 整片模式用单段 ~800 字符 prompt 性价比最高（goodcase 90 分案例即此形态），与逐镜模式互补 |
| 12 | **App 演示类 brief 的范式提示** | 视频模型渲染不出像素级一致的 UI，功能演示应走「真机录屏 + 只动画布」；当前分镜范式对这类 brief 是错配 |
| 13 | **keywords_en 注入去重** | `shotDetailUserPrompt` 把关键词逐字拼入，与场景描述重叠时（如 "clean ivory background"）出现重复 |
| 14 | **video 组未接线** | `llm.config.json` 的 video 配置目前无代码消费（M1 预留）；Agnes Video 2.5 Flash 的 `mode` 字段取值待确认 |

## 建议执行顺序

1. P0 #1（1 行修复）
2. P1 #2 + #3（重试 + 最小测试基建，半天内）
3. P1 #4/#5 文档同步（与上面同批）
4. P2 #6/#7/#9（体验与清理）
5. P3 按产品优先级排
