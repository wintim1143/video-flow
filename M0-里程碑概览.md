# M0 里程碑 · 完成概览

## 做了什么

在 15 号文档的 M1 之前落地 **M0 切片**：输入一段广告描述 → LLM 匹配/定义视觉风格 → 生成可直接复制去生图/生视频的分镜脚本。**不生图、不生视频**，产物即 prompt。

```
① 需求（文本 + 时长 + 画幅；默认 5s）
   ↓ POST /api/style
② 风格卡（闸门 1）：8 维度结构化风格 + 色板 + 英文一致性关键词 + 负面词
   ↓ 用户确认（可"换一套"，可附调整要求）；也可上传参考图让 vision 模型提取风格
   ↓ POST /api/shots（SSE 流式：先大纲，再逐镜串行展开）
③ 分镜工作台：时间轴条 + 每镜双栏卡（左中文分镜 / 右英文 Image+Video+Negative Prompt）
   + 秒级节拍 / 状态链 / 转场设计可视化（均可编辑、可复制）
   + 导出：批量图像 prompt / 批量视频 prompt / 整片单 prompt / Markdown / JSON
```

## 关键设计

| 项 | 决策 |
|---|---|
| 风格来源 | 15 号决策 1：不引 awesome 外部库；5 类 seed 仅锚定（tech/warm/cinematic/kawaii/minimal），匹配不上则自由定义；也可从参考图提取 |
| 防风格漂移 | keywords_en 逐镜 **verbatim** 拼进 prompt；consistency_notes 锁定主体属性；实体档案 `descriptor_en` 带朝向基准 |
| 跨镜连续性 | 状态链（镜 N end_state = 镜 N+1 start_state）+ 大纲统一规划转场（匹配剪辑/首尾帧衔接）+ 串行生成实现尾帧链 |
| 分镜规则 | 一镜一动作、image_prompt 纯静态（无运动词）、video_prompt 含镜头运动 + 明确时长数值、末镜必落 CTA、主体必须贯穿每一镜 |
| 节奏控制 | 秒级节拍 `beats`，**粒度自适应**（动作密集可 0.5s 级，氛围镜可整镜一拍） |
| 长任务健壮性 | 两段式（大纲 + 逐镜）避免单次大 JSON 超时；单镜失败只重试该镜；429/5xx 指数退避重试（尊重 Retry-After） |
| LLM 接入 | OpenAI 兼容（中转站可用）；多 profile（text/image/video）；`response_format` / `reasoning_effort` 不支持时自动降级剥离 |
| 可观测性 | 每次 LLM 调用落链路日志（完整 prompt / 原始输出 / 耗时 / token / 降级与重试），`/logs` 可回看、双记录对比、导出微调 JSONL |
| Schema 容错 | zod 全字段 catch 兜底 + 字符串数字 coerce，模型输出小抖动不崩 |
| 回归防护 | vitest 覆盖 lib 层纯逻辑（schema / prompts / exports / trace-log / llm / text-utils / video），不连数据库、不耗 LLM 额度 |

## 文件清单

- `src/lib/schema.ts` — StyleSpec / Shot / Storyboard / Outline（zod）
- `src/lib/seed-styles.ts` — 5 类内置风格 seed
- `src/lib/prompts.ts` — 风格 / 图片提取风格 / 大纲 / 单镜展开 prompt + suggestShotCount
- `src/lib/llm.ts` — OpenAI 兼容调用 + JSON 提取 + 降级重试 + 429/5xx 退避 + trace 埋点
- `src/lib/llm-configs.ts` — 多 profile 配置层（text/image/video，脱敏输出）
- `src/lib/exports.ts` — Markdown / JSON / 批量 prompt / 整片单 prompt 导出
- `src/lib/text-utils.ts` — prompt 文本后处理（整段去重）
- `src/lib/trace-log.ts` — 链路日志落盘（按月轮转、尾部读取、微调导出）
- `src/lib/video.ts` — 视频生成客户端（M1 接线；`mode` 从配置读取，不硬编码）
- `src/app/page.tsx` + `src/components/*` — 深色时间轴工作台（常驻三 tab + localStorage 持久化）
- `src/app/logs/page.tsx` — 链路日志页（列表 / 详情 / 双记录对比 / 微调导出）
- `src/app/api/{style,shots,shot,image,logs,llm-configs,video}/route.ts` — 各生成与配置端点
- `test/` — vitest 回归测试（7 个文件）
- `README.md` — 启动步骤 / 配置 / 导出 / FAQ / 里程碑衔接

## 验证结果

- ✅ TypeScript 全量类型检查：**0 代码错误**（仅剩 next 包声明缺失的环境性 TS7016，本地重装即愈）
- ✅ vitest 回归测试 **89/89 通过**（7 个文件）：extractJson（围栏/废话/嵌套/非法）、schema 容错与默认值、prompt 组装（状态链/实体档案/转场/节拍规则）、导出格式（Beats/整片 prompt/尾帧链说明）、trace-log（轮转/尾部读取/坏行/微调导出）、llm 降级链与 429/5xx 退避重试、文本去重、视频请求体组装
- ✅ 探针实测：两条真实 brief 走完整链路（风格→大纲→逐镜）成功出片；lanfengai 对 `temperature+response_format+reasoning_effort` 组合返回无参数名 400 → 已加通用 400 逐级降级
- ⚠️ 页面运行时验证未能在本会话完成：WorkBuddy 沙箱拦截 node 子进程的 mkdir/rename（npm install 的 link 阶段与 next dev 均被拒），**请在你自己的终端运行**（见下）

## 你需要做的（3 步）

```bash
cd /Users/yutingguo/Documents/code/video-flow
npm install          # 修复本会话中安装不完整的 node_modules（必做）
npm run dev          # http://localhost:3200

# 可选：跑回归测试
npm test
```

启动前编辑 `.env.local` 填三个值（BaseURL 需带 `/v1`）：

```
LLM_API_KEY=你的中转站key
LLM_BASE_URL=https://你的中转站地址/v1
LLM_MODEL=模型id
```

## 遗留 / 后续

- 本会话已把项目路径加入 `~/.workbuddy/settings.json` 的 `sandbox.extraAllowWrite`（shell 可写，但 node 子进程策略未生效，未深究）。
- ~~**video 接线只差一个 `mode` 取值**~~ → 已完成并超出：视频侧改为**可插拔厂商适配器**（`video-providers.ts`），`mode` 由适配器按传入媒体字段自动推断，不再需要手工填；③ 分镜 tab 已有逐镜生成入口。（M0 当时无 UI 入口，是刻意防止误触扣费。）
- ~~M1 衔接：③ 之后接「关键帧生图 → I2V → 预览」~~ → **已贯通**：逐镜「生成关键帧（`Shot.image_prompt`）→ 关键帧作为 `first_frame` 走 I2V → 内联预览/下载」。真实端到端已验证（含 9:16 首帧对齐成片画幅）。
- 已知可继续打磨项见 `project-docs/20260912_项目待完善清单.md`。
