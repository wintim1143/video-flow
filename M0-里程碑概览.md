# M0 里程碑 · 完成概览

## 做了什么

在 15 号文档的 M1 之前落地 **M0 切片**：输入一段广告描述 → LLM 匹配/定义视觉风格 → 生成可直接复制去生图/生视频的分镜脚本。**不生图、不生视频**，产物即 prompt。

```
① 需求（文本 + 时长 + 画幅）
   ↓ POST /api/style
② 风格卡（闸门 1）：8 维度结构化风格 + 色板 + 英文一致性关键词 + 负面词
   ↓ 用户确认（可"换一套"，可附调整要求）
   ↓ POST /api/shots
③ 分镜工作台：时间轴条 + 每镜双栏卡（左中文分镜 / 右英文 Image+Video+Negative Prompt 一键复制）
   + 导出：批量图像 prompt / 批量视频 prompt / Markdown / JSON（可下载）
```

## 关键设计

| 项 | 决策 |
|---|---|
| 风格来源 | 15 号决策 1：不引 awesome 外部库；5 类 seed 仅锚定（tech/warm/cinematic/kawaii/minimal），匹配不上则自由定义 |
| 防风格漂移 | keywords_en 逐镜 **verbatim** 拼进 prompt；consistency_notes 锁定主体属性 |
| 分镜规则 | 一镜一动作、image_prompt 纯静态（无运动词）、video_prompt 含镜头运动+时长、末镜必落 CTA |
| LLM 接入 | OpenAI 兼容（中转站可用），`.env.local` 配置；json_object 不支持时自动降级重试 |
| Schema 容错 | zod 全字段 catch 兜底 + 字符串数字 coerce，模型输出小抖动不崩 |

## 文件清单

- `src/lib/schema.ts` — StyleSpec / Shot / Storyboard（zod）
- `src/lib/seed-styles.ts` — 5 类内置风格 seed
- `src/lib/prompts.ts` — 风格 & 分镜两个 system prompt
- `src/lib/llm.ts` — OpenAI 兼容调用 + JSON 提取 + 降级重试
- `src/lib/exports.ts` — Markdown / JSON / 批量 prompt 导出
- `src/app/page.tsx` + `src/components/*` — 深色时间轴工作台（三阶段状态机）
- `src/app/api/style/route.ts`、`src/app/api/shots/route.ts` — 两个生成端点
- `README.md` — 启动步骤 / FAQ / 里程碑衔接

## 验证结果

- ✅ TypeScript 全量类型检查：**0 代码错误**（仅剩 next 包声明缺失的环境性报错，本地重装即愈）
- ✅ lib 层逻辑测试 **17/17 通过**：JSON 提取（围栏/废话/嵌套/非法）、schema 容错、时码、Markdown/批量导出、seed、prompt 模板
- ⚠️ 页面运行时验证未能在本会话完成：WorkBuddy 沙箱拦截 node 子进程的 mkdir/rename（npm install 的 link 阶段与 next dev 均被拒），**请在你自己的终端运行**（见下）

## 你需要做的（3 步）

```bash
cd /Users/yutingguo/Documents/code/video-flow
npm install          # 修复本会话中安装不完整的 node_modules（必做）
npm run dev          # http://localhost:3200
```

启动前编辑 `.env.local` 填三个值（BaseURL 需带 `/v1`）：

```
LLM_API_KEY=你的中转站key
LLM_BASE_URL=https://你的中转站地址/v1
LLM_MODEL=模型id
```

## 遗留 / 后续

- 本会话已把项目路径加入 `~/.workbuddy/settings.json` 的 `sandbox.extraAllowWrite`（shell 可写，但 node 子进程策略未生效，未深究）。
- M1 衔接：③ 之后接「关键帧生图 → I2V → 预览」，`StyleSpec` 与 `Shot.image_prompt` 直接复用；接 Mastra 时两个 route 的 prompt/schema 可原样搬进 workflow step。
