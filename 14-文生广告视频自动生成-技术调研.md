---
tags: [领域/Agent, AI/多智能体框架, 文生视频, 广告视频, 自动开发工作流]
created: 2026-09-04
---

# 文生广告视频自动生成 · 技术调研

> 一句话摘要：调研「AI 广告视频（30s–3min）自动生成」在 Mastra + Agent Skills 上的可行性。核心结论：**到 2026-09，没有任何模型能一步到位文本直生 30s–3min 连贯视频**（单次上限 Sora2=20s、Luma=18s、Runway/Kling/Pika=10s、Veo3.1=8s），3 分钟广告**必然 = 分镜拆解 + 逐镜先生关键帧图再图生视频 + 续写/拼接**。「先生图再生视频」在所有主流模型上都可行且是拿一致性的唯一手段。awesome-gpt-image-2 是**图像**风格库，不含视频时序/运动维度，需自行扩展 schema；分镜生成的开源 skill 已成熟不缺（ai-cinematic-video-director / PenShot / aivp-storyboard 等）。

## 〇、调研定位与背景

这是与 12/13 号（[[12-需求到成品AI链路与DESIGN.md多风格调研]]、[[13-需求到成品AI链路-需求确认文档]]）**同系列但独立的新需求调研**：

- 12/13 号 = **图像/UI 产品**链路（需求→PRD→线稿→高保真→成品代码）
- **本文档（14 号）= 视频广告链路调研**：文本/图 → 风格 → 分镜 → 视频生成

**用户本次诉求**：用 **Mastra** 编排，借鉴 **awesome-gpt-image-2** 的风格库，产出 **广告视频（30s–3min）**。两条输入路径：
1. 用户输入视频文本描述 → 系统从 awesome-gpt-image-2 选 1–3 个风格 → LLM 润色 → 生成 N 个分镜 → 生成视频
2. 用户输入一张图 → LLM 提取信息 → 定一个风格 → 后续同 1

**核心待答问题**：视频时长不长（30s–3min）时，还要不要先生图再从图生视频？还是一步到位？

---

## 一、模型能力边界：一步到位不可行（一手核验）

> 调研方法：仅用厂商官方文档，凡第三方博客传闻（"Veo4 30-60s""Luma 60s"）均标注**未证实**、不引用。

### 1.1 各主流模型官方单次时长上限（2026-09）

| 模型/版本 | 文生 T2V | 图生 I2V(首帧) | 首尾帧双图 | 单次最大时长 | 分辨率 | 续写/拼接手段 | 一手来源 |
|---|---|---|---|---|---|---|---|
| **Veo 3.1** (GA 2025-11) | ✅ | ✅ | ✅ from first&last | **4/6/8s**（I2V 仅 8s） | 720p/1080p/**4K** | Extend videos | [Vertex AI](https://cloud.google.com/vertex-ai/generative-ai/docs/models/veo/3-1-generate) |
| **Sora 2** | ✅ | ✅ image ref | ⚠️ 角色复用，无显式首尾帧对 | **20s** | 最高 1080×1920(Pro) | video extensions | [OpenAI](https://developers.openai.com/api/docs/guides/video-generation/) ⚠️ **API 2026-09-24 停用** |
| **Kling 2.1** (可灵) | ✅ | ✅ 必填首帧 | ✅ **首帧+尾帧双图**(pro/v2-6) | T2V 5/10s；I2V 3–15s；参考模式最长 **30s** | **1080p** | chain generations(尾帧作下镜首帧) | [Kling API](https://docs.ksyun.com/documents/44983) |
| **Runway Gen-4.5** | ✅(Gen4.5加回；Gen4 纯I2V) | ✅ 首帧 | ⚠️ 仅首帧 | 2–10s | 720p | Aleph2.0 编辑最长 30s | [Gen-4.5](https://help.runwayml.com/hc/en-us/articles/46974685288467-Creating-with-Gen-4-5) |
| **Pika** | ✅ | ✅ | ⚠️ PikaFrames 2–5 关键帧 | I2V 5/10s | 720p/1080p | extend +4s | [PikaFrames](https://dev.pika.art/models/pika/pikaframes/image-to-video) |
| **Luma Ray3.14** | ✅ | ✅ start/end keyframes | ✅ start/end | Ray2 5/10s(Extend≈30s)；Ray3.14 Modify 18s | 1080p/HDR | Keyframes+Extend | [Luma](https://support.lumalabs.ai/news/ray3_14) |

### 1.2 直接回答用户的疑问

> **30s–3min 广告视频，还要不要先生图再图生视频？**
> **结论：要先生成关键帧/分镜图，不要指望一步到位。** 理由（均有官方依据）：
> 1. 一步到位的文本长视频被**官方时长上限（≤20s）**卡死，3 分钟必须分段；
> 2. 广告要**品牌一致性**（人物脸/产品/Logo/画风跨镜统一），只能靠「首帧/参考图 + 角色复用」约束，文本直生无法保证；
> 3. **首尾帧双图驱动**是官方提供的最佳拼接手段（Veo/Kling 显式支持、Luma start/end），先有尾帧图才能与下镜首帧无缝衔接；
> 4. "先生图"不一定额外跑图像模型——多数视频模型自身接受首帧图做 I2V。**高一致性镜头（产品主视觉/人物定妆）额外用图像模型产 hero 关键帧**，普通转场镜头直接用视频模型首帧接口即可。

### 1.3 模型选型速记（受能力约束）

- 要**最长单镜 + 角色复用** → Sora2（20s，但 API 即将停用，生产慎用）
- 要**首尾帧精确拼接 + 1080p 产品动效** → **Kling 2.1**（首尾帧开关、chain、运动笔刷）
- 要**画质/4K + 精品短片段** → Veo 3.1（8s/镜、Extend、4K）
- 要**精细后期编辑/表演** → Runway Gen-4.5 + Aleph/Act-Two（720p，编辑最长 30s）
- 要**多关键帧分镜转场** → Pika PikaFrames
- 要 **HDR/电影感预演** → Luma Ray3.14（1080p/HDR）

---

## 二、分镜(storyboard)生成的开源提示词/框架——已成熟，不缺

### 2.1 现成项目（多数遵循 Agent Skills 规范，可直接装 Mastra）

| 项目 | 形态 | 分镜输出结构 | 来源 |
|---|---|---|---|
| **ai-cinematic-video-director** (Adityaraj0421) | Claude 插件/4 skill | 6 要素 **WHO/WHERE/ACTION/CAMERA/MOOD/PACING** + 5 规则；产出 timeline/audio plan/production notes | [github](https://github.com/Adityaraj0421/ai-cinematic-video-director-claude-skill) |
| **PenShot / story-shot-agent** (neopen) | Python + LangGraph + MCP | 剧本→JSON `fragments[]`（含 prompt/negative/duration/style/audio），向量检索保跨镜头一致性 | [github](https://github.com/neopen/story-shot-agent) |
| **chunpu/agent-skills** | Agent Skills 批量 | `text-storyboard`(小说→视觉分镜)、`generate-film-video-prompt`、`novel-to-video` | [github](https://github.com/chunpu/agent-skills) |
| **aivp-storyboard** (AIVidPipeline) | `npx skills add` | 逐 shot：景别/时长/action/visual + camera 运动 + SCELA 格式 keyframe + 转场 | [aividpipeline](https://aividpipeline.com/es/skills/aivp-storyboard) |
| **ai-video-storyboard** (cajias) | Claude 插件 | 面向 >15s 视频；先锁 **Visual Consistency Layer**(palette/lighting/lens/film-look/motion)，再按 ~5s/shot 切时间轴 | [claudepluginhub](https://claudepluginhub.com/skills/cajias-ai-video-storyboard-plugins-ai-video-storyboard/ai-video-storyboard) |
| **Open-AI-Micro-Drama-Generator** (Anil-matcha) | 多 agent 流水线 | screenwriter→character→scene scripting→storyboard(每场景 3–5 shots)→frame→video→moviepy 拼接 | [github](https://github.com/Anil-matcha/Open-AI-Micro-Drama-Generator) |
| **ai-video-pipeline** (edo1z) | 本地 Python | 主题→`scenes[]`→gpt-image-1 生图→TTS→字幕→ffmpeg；`--motion` 接 Sora 做 I2V | [github](https://github.com/edo1z/ai-video-pipeline) |

> **结论：用户方案第 3 步（生成 N 个分镜）不缺现成 skill**，最贴合的是 ai-cinematic-video-director(storyboard-to-video)、aivp-storyboard、chunpu/text-storyboard、PenShot。

### 2.2 视频提示词比图像多哪些维度

图像提示词（awesome-gpt-image-2 原子 schema）：`subjects/lighting/materials/layout/visual details + style`（单帧静态）。
视频在其上必须增加：

1. **运动/动作 (motion/action)**：一个 clip 一个动作（ai-cinematic 规则）
2. **镜头语言 (camera)**：景别 wide/medium/close-up + 镜头运动 dolly/pan/tracking/orbit/static/drone
3. **时间推进/跨镜头连续性 (temporal continuity)**：先锁 palette & lighting 再逐镜写
4. **转场 (transitions)**：cut/dissolve/L-cut/match-cut + 时机
5. **时长 (duration)**：约 5s/shot
6. **角色一致性 (character consistency)**：锁定属性 verbatim 粘贴 / 参考图 / 向量检索
7. **口播/字幕/音频 (audio/voiceover)**：music/ambience/SFX + 入出点
8. **运动负面词 (negative_prompt for motion)**

**结构化分镜的主流写法** = 逐 shot JSON/表格，字段约 `shot_id / shot_type(景别) / camera_movement / subject / action / visual_description / duration / transition / audio / negative_prompt`。

---

## 三、awesome-gpt-image-2 复用到视频——不可直接用，需扩展 schema

### 3.1 一手核验它缺什么

已核对 `data/style-library.json` 实测结构（一手提取）：
- `styles[]` 每条**只有 4 字段** `{id, value, title{en,zh}, keywords[]}` —— 风格层只是"标签+关键词"，**无 composition/lighting/materials 维度**
- 丰富维度（subjects/lighting/materials/layout）以**自由文本**在 `templates[]` 的 `guidance` 和 `tags` 里；共 **22 个模板，全是静态图像模板**
- 核心理念 "Prompt as Code" 目前**只覆盖图像**

**复用到视频必然缺失**：运动、镜头运动、时间推进/跨镜连续、转场、跨帧角色一致性、音频/口播。

### 3.2 社区是否已有迁移？——没有，但"先图后动画"路径被验证

- **诚实结论**：未检索到开源项目把 awesome-gpt-image-2 迁移成视频风格库
- 但商业产品（Imgo/Vddo/Samsar Vidgenie）验证了"用 GPT Image2 生参考帧 → route 到 Sora2/Veo3/Kling 做 I2V"的路径
- 与 ai-cinematic 规则 #1 **"START FRAME FIRST——先出图再动画"** 一致

### 3.3 静态图模板 → 视频 prompt 的改造映射（推荐做法）

> 无单一官方文档给出该映射，以下是综合社区 skill 规范归纳的可引用做法。

| 图像模板维度 | 视频改造：追加后缀/维度 | 来源 |
|---|---|---|
| `subject` 主体 | + 锁定角色/物体属性（逐镜 verbatim，一致性） | ai-cinematic 规则#4 / PenShot |
| `composition` 构图 | + 景别 + 镜头运动(wide/medium/close + dolly/pan/tracking…) | aivp-storyboard / cajias |
| `lighting` 光照 | + 时间推进 + 跨镜光线色彩连续（先锁 palette） | cajias Visual Consistency Layer |
| `style` 风格 | + film look(16mm grain/anamorphic/35mm) + motion language(handheld/locked-off/gimbal) | cajias |
| `materials/layout` | 基本保留，作为**首帧(start frame)静态描述** | ai-cinematic 规则#1 |
| (图模板没有) | **新增** duration / transition / audio / negative_prompt | cajias 节奏表 / aivp / PenShot |

**推荐落地公式**（可直接写进 storyboard skill）：
> **视频 shot prompt = [awesome-gpt-image-2 的 style 标签] + [subject 锁定属性] + [composition→景别+运动] + [lighting→时间/连续] + [action 单动作] + [duration] + [transition] + [audio/voiceover] + [negative_prompt]**
>
> 与 ai-cinematic-video-director 的 6 要素（WHO/WHERE/ACTION/CAMERA/MOOD/PACING）高度吻合，可作为"把图像风格库翻译成视频分镜"的填充模板。

---

## 四、Mastra 落地架构（一手：docs.mastra.ai）

### 4.1 Workflow 编排 5 步流水线

```ts
createWorkflow({ id, inputSchema, outputSchema })
  .then(step选风格)   // 调 Agent，挂 gpt-image-2-style-library skill
  .then(step润色)     // 基于选中风格润色描述
  .then(step生成分镜) // 调 Agent，挂 aivp-storyboard / chunpu/text-storyboard
  .then(step逐镜生视频) // .parallel() 扇出，每 shot 调视频 tool/MCP
  .then(step拼接成片)  // ffmpeg tool
  .commit()
```

- 核心 API：`createStep({ id, inputSchema, outputSchema, execute })` + `.then()/.branch()/.parallel()/.foreach()`，`stateSchema` 跨步共享状态
- step 里调 agent：`mastra.getAgent('x').stream()`
- 结果 discriminated union：`success/failed/suspended/tripwire/paused`
- 来源：https://mastra.ai/docs/workflows/overview

### 4.2 图像 vs 视频集成现状

- **图像生成**：半内置，AI SDK v7 提供 `openai.tools.imageGeneration()`（PR #19430, @mastra/core 1.52.0）
- **视频生成**：**无官方原生集成**，三选一：
  1. 自建 HTTP tool：`createTool` + `fetch` 直连视频 API（Veo/Kling/Runway/Replicate）
  2. MCP server：如 **Composio Veo MCP for Mastra**（Gemini API 生成 8s/720p、监控 job、下载结果）
  3. Replicate 等也走 HTTP tool
- 来源：https://mastra.ai/blog/ai-sdk-v7-support · https://composio.dev/toolkits/veo/framework/mastra-ai

### 4.3 长耗时视频任务（几十秒~几分钟）处理——两套互补机制

**机制 1：Workflow suspend/resume**（适合确定性流水线 + 长任务 + 人审）
- step 内 `suspend(payload)` → 执行快照持久化到 storage（LibSQL），**跨部署/重启存活**；`run.resume({ step, resumeData })` 恢复
- Client 支持 fire-and-forget：`run.start()` 立即返回，后 `run.runById()` 轮询 / `resumeAsync()`
- 来源：https://mastra.ai/docs/workflows/suspend-and-resume

**机制 2：Agent Background tasks**（适合 agent 内长轮询/等 webhook/人审）
- tool 设 `background:{enabled:true}`，`execute` 里 `suspend(data)` 暂停并释放并发槽位；`mastra.backgroundTaskManager.resume(taskId, resumeData)` 恢复
- 来源：https://mastra.ai/docs/agents/background-tasks

**针对视频流水线推荐**：
- 每个"生视频" step 调慢速异步 API：
  - (a) **step 内轮询**：execute 里循环 fetch provider job 状态直到 done（abortSignal 响应取消）——适合几十秒
  - (b) **suspend + 外部恢复**：提交生成后 `suspend({jobId})`，webhook/worker 视频产出后 `resume()` 带结果 URL——适合几分钟级、需跨重启持久化
- 人审闸门（确认分镜/风格再花 credits）：用 suspend/resume 做 HITL

### 4.4 Agent Skills 接法（关键）

Mastra **原生支持 Agent Skills 规范**：
- **Workspace skills**（filesystem-backed）：skill 目录放 `skills:['skills']`，agent 自动发现，提供 `skill/skill_read/skill_search` 三工具（progressive disclosure）
- **First-class agent skills**：`createSkill({ name, description, instructions })` 挂 agent `skills:[]`
- 来源：https://mastra.ai/docs/workspace/skills

> 含义：**awesome-gpt-image-2 style-library SKILL.md + §二 的视频分镜 SKILL.md 都能直接丢进 Mastra workspace skills 目录被 agent 自动发现**——这是接分镜 skill 进流水线的最简路径。

---

## 五、对用户方案的修正与落地建议

### 5.1 方案是否成立

用户的"先生成 N 个分镜再生成视频"方向**完全正确且是业界主流**。但需修正三点：

1. **"分镜直接驱动生视频"要改成"分镜先生关键帧图，再图生视频"**——因为 30s+ 无法文本一步到位，且广告一致性必须靠参考帧/首尾帧。
2. **第 1、3 步（选风格、生成分镜）有现成开源 skill**，不用从零写：选风格挂 awesome-gpt-image-2 style-library，分镜挂 ai-cinematic-video-director / aivp-storyboard。
3. **awesome-gpt-image-2 只能当"选风格+首帧图风格"的输入**，需按 §3.3 公式扩展成视频分镜 schema（补 motion/camera/duration/transition/audio），不能当现成视频风格库。

### 5.2 建议的完整流水线

```
输入：文本描述 或 参考图
  → ① 风格选定 Agent  挂 gpt-image-2-style-library skill
       文本:匹配1-3风格; 图:LLM提取信息→定1风格
  → 【闸门·用户确认风格】(可选, suspend)
  → ② 润色 Agent  基于风格润色描述
  → ③ 分镜 Agent  挂 aivp-storyboard / text-storyboard
       产出 N 个分镜 JSON (含首尾帧衔接设计)
  → 【闸门·用户确认分镜】(suspend, 防乱花 credits)
  → ④ 关键帧 Agent  高一致性镜头用图像模型产首/尾帧图
  → ⑤ 逐镜生视频    .parallel() 每镜调 I2V API(带首帧[+尾帧])
       长任务用 suspend/resume 或 step 内轮询
  → ⑥ 拼接/后期    ffmpeg 拼接 + TTS配音 + 字幕 + 放大
  → ⑦ 一致性 QA    跨镜校验角色/产品/Logo
```

### 5.3 技术选型倾向（受模型能力约束）

- **视频主引擎倾向 Kling 2.1**（首尾帧双图 + 1080p + chain 拼接 + 运动笔刷，最贴广告）；画质优先可切 **Veo 3.1**（4K，8s/镜需更多镜头）
- **先生帧图**：图生可复用 awesome-gpt-image-2 风格的图像模型（GPT-Image2 / FLUX）
- **分镜 skill**：ai-cinematic-video-director 或 aivp-storyboard（现成、可装 Mastra）
- **注意 Sora2 API 2026-09-24 停用**，生产系统别长期依赖

---

## 关联

- 姊妹篇（图像/UI 产品链路）：[[12-需求到成品AI链路与DESIGN.md多风格调研]] · [[13-需求到成品AI链路-需求确认文档]]
- 产品迭代内核：[[10-实习生产品开发工具设计文档]]
- 工程外壳编排：[[11-IM驱动的多Agent自动开发工作流设计]]
